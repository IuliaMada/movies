<p id="notice"><%= notice %></p>
<table>
  <tr>
    <td>
      <p>
        <strong>Title:</strong>
        <%= @movie.title%>
      </p>
      <p>
        <%=image_tag @movie.poster%>
      </p>
    </td>
    <td>
      <p>
        <strong>Genre:</strong>
        <% @movie.genres.each do |g|%>
        <%= g.category %>
        <% end %>
      </p>
      <p>
        <strong>Countries:</strong>
        <%= %>
      </p>
      <p>
        <strong>Rating:</strong>
        <%= @movie.title%>
      </p>
      <p>
        <strong>Plot:</strong>
        Bla bla bla blaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      </p>
      <p>
        <strong>Runtime:</strong>
        <%= @movie.duration%>
      </p>
      <p>
        <strong>Comments:</strong>
        <%= @movie.duration%>
      </p>
      <p>
        <strong>I like this movie:</strong>
        	Ion,Marie,Andreis
      </p>
    </td>
    <td>
      <% if lists_user(current_user.id).include?(@movie.id) %>
        <br>
        <p> Deja se afla intr-o lista </p>
      <% else %>
          <%= form_tag user_lists_path, :action => "create", :method => "post"  do %>
          <%= hidden_field_tag :movie_id, @movie.id %>
          <%= hidden_field_tag :status, true  %>
          <%= hidden_field_tag :user_id, current_user.id  %>
          <%= submit_tag "Musai de vazut" %>
          <% end %>
        
          <%= form_tag user_lists_path, :action => "create", :method => "post" do %>
          <%= hidden_field_tag :movie_id, @movie.id  %>
          <%= hidden_field_tag :status , false  %>
          <%= hidden_field_tag :user_id, current_user.id  %>
          <%= submit_tag "Vazut"  %>
          <% end %>
      <% end %>
    </td>
  </tr>
  <tr>
    <td>
      <%= form_tag comments_path, :action => "create", :method => "post" do  %>
        <div class="field">
          <%= hidden_field_tag :user_id, current_user.id  %>
        </div>
        <div class="field">
          <%= label_tag :body %><br>
          <%= text_area_tag :body %>
        </div>
        <div class="field">
          <%= hidden_field_tag :movie_id, @movie.id  %>
        </div>
        <div class="actions">
          <%= submit_tag "Save comment" %>
        </div>
      <% end %>
    </td>
  </tr>

</table>
<div id="comments"></div>



<%= button_to "Show comments (#{@movie.comments.count})", {controller: "comments", action: "show",id: @movie }, method: "get", remote: true %>




<%= link_to "Home" ,movies_path %>
<%= link_to "New comment",  new_comment_path %>

