$('#add_comment').html("<%= escape_javascript (render partial: 'add_comment', :object => @comment, :as => 'comment') %>")
 $('#new_comment :submit').click(function() {
 	$('#new_comment').hide();
 });