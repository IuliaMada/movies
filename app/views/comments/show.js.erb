var comments = $('#comments');
if (comments.is(':empty')) {
	comments.html("<%= escape_javascript (render partial: 'comment', collection: @movie_comments) %>");
	$('.showbutton').val('Hide Comments');
} else {
	comments.empty();
	$('.showbutton').val('Show Comments (<%= @movie_comments.count %>)');
}


