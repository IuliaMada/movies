<%= form_for comment, :action => "create", :method => "post", remote: true do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id  %>
  <%= f.hidden_field :movie_id, value: comment.movie_id  %>
  <%= f.text_area :body %>
  <%= f.submit "Save comment" %>       
<% end %>

